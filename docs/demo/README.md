This folder is a **frozen copy** of a full pipeline run (Astra Vey — *Prismatic Duelist*), checked in so **GitHub Pages** can host the layered `viewer.html` and PNGs.

- **Do not** point the README generator here by default; local development still writes to `output_card/` (gitignored).
- To refresh the hosted demo after a new generation, copy your new `output_card/*` over this directory and commit.
